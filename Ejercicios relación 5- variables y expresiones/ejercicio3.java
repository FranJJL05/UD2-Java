package relación5variables;

public class ejercicio3 {
    public static void main (String args[]){
    // 3-
    // a)
    double resultado = (3.0 / 2.0) + (4.0 / 3.0);

    // b)
    double resultado = (1.0 / x - 5.0) - (3.0 * x * y / 4.0);

    // c)
    double resultado = (1.0 / 2.0) + 7.0;

    // d)
    double resultado = 7.0 + (1.0 / 2.0);

    // e)
    double resultado = ((a * a) / b - c) + (d - e) / (f - ((g * h) / j));

    // f)
    double resultado = (m / n) + p;

    // g)
    double resultado = m + (n / (p - q));

    // h)
    double resultado = (a * a) / (b * b) + (c * c) / (d * d);

    // i)
    double resultado = (m + n / p) / (q - r / s);

    // j)
    double resultado = (3.0 * a + b) / ((c - (d + 5.0 / e)) / (f + g / (2.0 * h)));

    // k)
    double resultado = ((a * a) + (2.0 * a * b) + (b * b)) / (((1.0 * x) / 2.0) + 2.0);

}
}
